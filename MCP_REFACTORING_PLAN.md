# План модернизации MCP-интерфейса 1С

> Документ — единый источник правды по правилам именования путей и типов
> для MCP-инструментов расширения `v8vscedit`. Любой агент, читающий
> описание инструмента, должен иметь полный набор правил, чтобы не гадать.

---

## Решения пользователя

1. **Корень — множественное число везде в путях, единственное — только в текстах запросов 1С.**
   - Путь: `Справочники.Контрагенты`, `Документы.РасходТовара`, `РегистрыСведений.КурсыВалют`.
   - В запросах (отдельно от MCP): `ВЫБРАТЬ … ИЗ Справочник.Контрагенты` — это не задача MCP.
2. **Реквизиты и табличные части идут напрямую**, без сегмента-роли.
   - `Справочники.Контрагенты.ИНН` — реквизит.
   - `Справочники.Контрагенты.КонтактныеЛица` — табличная часть.
   - Внутри табличной части сегменты возвращаются: `Справочники.Контрагенты.ТабличнаяЧасть.КонтактныеЛица.Реквизит.ФИО`.
3. **Подсистемы без удвоения сегмента:** `Подсистема.Продажи.Розница.Возвраты`.
4. **Английские алиасы на входе не поддерживаются.** Никаких `Catalog.X` / `Catalogs.X`.
5. **Legacy-формы (типы 7.7/8.0) не поддерживаются.**
6. **`УникальныйИдентификатор` / `UUID`** — добавить после эмпирической проверки реальной XML-сериализации в 1С 8.3.

---

## Глава 0. Принципы канона

| Принцип | Конкретика |
|---|---|
| Язык | Только русский. |
| Число корня | Множественное (`Справочники`, `Документы`, `РегистрыСведений`) для коллекций. `Подсистема`, `Конфигурация`, `Расширение` — единственное. |
| Регистр | CamelCase, **без пробелов** во всех сегментах. |
| Разделитель | Точка. Точка внутри имени запрещена. |
| Прямые дети | Реквизит и ТабличнаяЧасть — без сегмента. |
| Сегментированные дети | СтандартныйРеквизит, Форма, Команда, Макет — с сегментом. |
| Внутри ТЧ | Сегменты возвращаются: `ТабличнаяЧасть.Имя.Реквизит.Имя`. |
| Регистры | Дочерние Измерение/Ресурс/Реквизит — **прямо**, без сегмента. Имена в 1С уникальны в рамках регистра, конфликт исключён. |
| Перечисления | Значения — прямо: `Перечисления.СтатусыЗаказа.Активен`. |
| Журналы документов | Графы — прямо: `ЖурналыДокументов.Складские.Контрагент`. |
| Задачи | Реквизит адресации — с сегментом: `Задачи.X.РеквизитАдресации.Y`. |
| Планы счетов | Признак учёта — с сегментом: `ПланыСчетов.X.ПризнакУчета.Y`. |
| Модули | Сегмент в хвосте: `Справочники.X.МодульОбъекта`, `Справочники.X.Форма.Y.МодульФормы`. |
| Подсистемы | Без удвоения: `Подсистема.А.Б.В`. |
| Типы значений | Единственное число + суффикс: `СправочникСсылка.X`, `СправочникОбъект.X`, `РегистрСведенийНаборЗаписей.X`. |
| Выдача | Только канон. Никаких параллельных алиасов. |

---

## Глава 1. Канонические пути — полная справочная таблица

### 1.1. Корни (коллекции метаданных)

| Корневой сегмент | Тип метаданных | XML-папка |
|---|---|---|
| `Подсистема` | Subsystem | `Subsystems` |
| `ОбщиеМодули` | CommonModule | `CommonModules` |
| `ПараметрыСеанса` | SessionParameter | `SessionParameters` |
| `Роли` | Role | `Roles` |
| `ОбщиеРеквизиты` | CommonAttribute | `CommonAttributes` |
| `ПланыОбмена` | ExchangePlan | `ExchangePlans` |
| `КритерииОтбора` | FilterCriterion | `FilterCriteria` |
| `ПодпискиНаСобытия` | EventSubscription | `EventSubscriptions` |
| `РегламентныеЗадания` | ScheduledJob | `ScheduledJobs` |
| `ФункциональныеОпции` | FunctionalOption | `FunctionalOptions` |
| `ПараметрыФункциональныхОпций` | FunctionalOptionsParameter | `FunctionalOptionsParameters` |
| `ОпределяемыеТипы` | DefinedType | `DefinedTypes` |
| `ХранилищаНастроек` | SettingsStorage | `SettingsStorages` |
| `ОбщиеФормы` | CommonForm | `CommonForms` |
| `ОбщиеКоманды` | CommonCommand | `CommonCommands` |
| `ГруппыКоманд` | CommandGroup | `CommandGroups` |
| `ОбщиеМакеты` | CommonTemplate | `CommonTemplates` |
| `ОбщиеКартинки` | CommonPicture | `CommonPictures` |
| `XDTOПакеты` | XDTOPackage | `XDTOPackages` |
| `WebСервисы` | WebService | `WebServices` |
| `WSСсылки` | WSReference | `WSReferences` |
| `HTTPСервисы` | HTTPService | `HTTPServices` |
| `СервисыИнтеграции` | IntegrationService | `IntegrationServices` |
| `ЭлементыСтиля` | StyleItem | `StyleItems` |
| `Стили` | Style | `Styles` |
| `Языки` | Language | `Languages` |
| `Константы` | Constant | `Constants` |
| `Справочники` | Catalog | `Catalogs` |
| `Документы` | Document | `Documents` |
| `ЖурналыДокументов` | DocumentJournal | `DocumentJournals` |
| `Нумераторы` | DocumentNumerator | `DocumentNumerators` |
| `Последовательности` | Sequence | `Sequences` |
| `Перечисления` | Enum | `Enums` |
| `Отчеты` | Report | `Reports` |
| `Обработки` | DataProcessor | `DataProcessors` |
| `ПланыВидовХарактеристик` | ChartOfCharacteristicTypes | `ChartsOfCharacteristicTypes` |
| `ПланыСчетов` | ChartOfAccounts | `ChartsOfAccounts` |
| `ПланыВидовРасчета` | ChartOfCalculationTypes | `ChartsOfCalculationTypes` |
| `РегистрыСведений` | InformationRegister | `InformationRegisters` |
| `РегистрыНакопления` | AccumulationRegister | `AccumulationRegisters` |
| `РегистрыБухгалтерии` | AccountingRegister | `AccountingRegisters` |
| `РегистрыРасчета` | CalculationRegister | `CalculationRegisters` |
| `БизнесПроцессы` | BusinessProcess | `BusinessProcesses` |
| `Задачи` | Task | `Tasks` |
| `ВнешниеИсточникиДанных` | ExternalDataSource | `ExternalDataSources` |

### 1.2. Псевдо-корни (один экземпляр)

| Путь | Что это |
|---|---|
| `Конфигурация` | Узел корня основной конфигурации, для свойств уровня конфигурации. |
| `Расширение` | Узел корня расширения. |

### 1.3. Дочерние сегменты — прямые (без префикса)

| Контекст | Что разрешено напрямую |
|---|---|
| Справочник, Документ, ПВХ, ПВР, ПС, БП, Задача, Отчёт, Обработка | `Реквизит` |
| те же | `ТабличнаяЧасть` |
| РегистрСведений, РегистрНакопления, РегистрБухгалтерии, РегистрРасчёта | `Измерение`, `Ресурс`, `Реквизит` |
| Перечисление | `ЗначениеПеречисления` |
| ЖурналДокументов | `Графа` |
| Подсистема | вложенная `Подсистема` |

**Принцип:** имена в этих категориях уникальны внутри объекта (1С это гарантирует), поэтому сегмент-роль не нужен.

### 1.4. Дочерние сегменты — с префиксом (обязательно)

| Сегмент пути | Где |
|---|---|
| `СтандартныйРеквизит` | псевдо-реквизиты (`Код`, `Наименование`, `Ссылка`, `Дата`, `Номер`, …) |
| `Форма` | везде, где есть формы |
| `Команда` | везде, где есть команды |
| `Макет` | везде, где есть макеты |
| `Реквизит` | **внутри** `ТабличнаяЧасть.Имя` |
| `РеквизитАдресации` | у Задачи |
| `ПризнакУчета` | у ПланСчетов |
| `ПризнакУчетаСубконто` | у ПланСчетов |
| `Перерасчет` | у РегистрРасчёта |
| `Таблица`, `Куб`, `Функция` | у ВнешнегоИсточникаДанных |

### 1.5. Модули — сегмент в хвосте

| Путь | Файл BSL |
|---|---|
| `Справочники.X.МодульОбъекта` | `Catalogs/X/Ext/ObjectModule.bsl` |
| `Справочники.X.МодульМенеджера` | `Catalogs/X/Ext/ManagerModule.bsl` |
| `Документы.X.МодульОбъекта` / `МодульМенеджера` | аналогично |
| `РегистрыСведений.X.МодульНабораЗаписей` | `InformationRegisters/X/Ext/RecordSetModule.bsl` |
| `РегистрыСведений.X.МодульМенеджера` | `InformationRegisters/X/Ext/ManagerModule.bsl` |
| `Константы.X.МодульМенеджераЗначения` | `Constants/X/Ext/ValueManagerModule.bsl` |
| `Справочники.X.Форма.Y.МодульФормы` | `Catalogs/X/Forms/Y/Ext/Form/Module.bsl` |
| `Справочники.X.Команда.Z.МодульКоманды` | `Catalogs/X/Commands/Z/Ext/CommandModule.bsl` |
| `ОбщиеМодули.X` | `CommonModules/X/Ext/Module.bsl` (модуль = сам объект) |
| `ОбщиеФормы.X.МодульФормы` | `CommonForms/X/Ext/Form/Module.bsl` |
| `ОбщиеКоманды.X.МодульКоманды` | `CommonCommands/X/Ext/CommandModule.bsl` |
| `WebСервисы.X.МодульМенеджера` | `WebServices/X/Ext/Module.bsl` |
| `HTTPСервисы.X.МодульМенеджера` | `HTTPServices/X/Ext/Module.bsl` |
| `Конфигурация.МодульУправляемогоПриложения` | `Ext/ManagedApplicationModule.bsl` |
| `Конфигурация.МодульСеанса` | `Ext/SessionModule.bsl` |
| `Конфигурация.МодульВнешнегоСоединения` | `Ext/ExternalConnectionModule.bsl` |
| `Конфигурация.МодульОбычногоПриложения` | `Ext/OrdinaryApplicationModule.bsl` |

### 1.6. Примеры законченных путей

```
Справочники.Контрагенты
Справочники.Контрагенты.ИНН                                  ← реквизит
Справочники.Контрагенты.СтандартныйРеквизит.Код               ← стандартный реквизит
Справочники.Контрагенты.КонтактныеЛица                        ← табличная часть
Справочники.Контрагенты.ТабличнаяЧасть.КонтактныеЛица.Реквизит.ФИО
Справочники.Контрагенты.Форма.ФормаСписка
Справочники.Контрагенты.Форма.ФормаЭлемента.МодульФормы
Справочники.Контрагенты.Команда.Печать
Справочники.Контрагенты.Команда.Печать.МодульКоманды
Справочники.Контрагенты.Макет.СчетФактура
Справочники.Контрагенты.МодульОбъекта
Справочники.Контрагенты.МодульМенеджера

РегистрыСведений.КурсыВалют
РегистрыСведений.КурсыВалют.Валюта                            ← измерение
РегистрыСведений.КурсыВалют.Курс                              ← ресурс
РегистрыСведений.КурсыВалют.Комментарий                       ← реквизит
РегистрыСведений.КурсыВалют.МодульНабораЗаписей

Перечисления.СтатусыЗаказа.Активен                            ← значение перечисления

ЖурналыДокументов.Складские.Контрагент                        ← графа

Задачи.ВыполнитьПоручение.РеквизитАдресации.Исполнитель

ПланыСчетов.Основной.ПризнакУчета.Количественный

РегистрыРасчета.Зарплата.Перерасчет.Сторнирование

ОбщиеМодули.ОбщегоНазначения                                  ← сам общий модуль
ОбщиеКоманды.ГлобальныйПоиск.МодульКоманды
ОбщаяФорма.ФормаНастроек.МодульФормы

Подсистема.Продажи
Подсистема.Продажи.Розница
Подсистема.Продажи.Розница.Возвраты

Конфигурация
Конфигурация.МодульУправляемогоПриложения
```

---

## Глава 2. Реестр типов значений (`v8vscedit_list_available_types` / `v8vscedit_set_type`)

### 2.1. Базовые платформенные типы

| Канон (рус) | XML | Сейчас есть? | Контекст |
|---|---|---|---|
| Строка | String | ✓ | оба |
| Число | Number | ✓ | оба |
| Булево | Boolean | ✓ | оба |
| Дата | Date | ✓ | оба |
| ДатаВремя | DateTime | ✓ | оба |
| ХранилищеЗначения | ValueStorage | ✓ | оба |
| **УникальныйИдентификатор** | **UUID** | ✗ | оба (см. §2.4) |
| **ДвоичныеДанные** | **BinaryData** | ✗ | оба |
| **Тип** | **Type** | ✗ | реквизит формы |
| **ОписаниеТипов** | **TypeDescription** | ✗ | реквизит формы |
| **СписокЗначений** | **ValueList** | ✗ | только формы |
| **ТаблицаЗначений** | **ValueTable** | ✗ | только формы |
| **ДеревоЗначений** | **ValueTree** | ✗ | только формы |
| **ТабличныйДокумент** | **SpreadsheetDocument** | ✗ | только формы |
| **Картинка** | **Picture** | ✗ | оба |
| **ФорматированнаяСтрока** | **FormattedString** | ✗ | только формы |

Контекст:
- «оба» — допустим как тип реквизита прикладного объекта и как тип реквизита формы.
- «только формы» — нельзя как тип реквизита метаданных.

### 2.2. Производные ссылочные типы (генерируются по конфигурации)

| Корневой тип метаданных | Производные (русский ↔ XML) |
|---|---|
| Справочник | СправочникСсылка↔CatalogRef, СправочникОбъект↔CatalogObject, СправочникМенеджер↔CatalogManager, СправочникВыборка↔CatalogSelection, СправочникСписок↔CatalogList |
| Документ | ДокументСсылка↔DocumentRef, ДокументОбъект↔DocumentObject, ДокументМенеджер↔DocumentManager, ДокументВыборка↔DocumentSelection, ДокументСписок↔DocumentList |
| Перечисление | ПеречислениеСсылка↔EnumRef, ПеречислениеМенеджер↔EnumManager, ПеречислениеСписок↔EnumList |
| ПланВидовХарактеристик | ПланВидовХарактеристикСсылка/Объект/Менеджер↔ChartOfCharacteristicTypesRef/Object/Manager |
| ПланСчетов | ПланСчетовСсылка/Объект/Менеджер↔ChartOfAccountsRef/Object/Manager |
| ПланВидовРасчета | ПланВидовРасчетаСсылка/Объект/Менеджер↔ChartOfCalculationTypesRef/Object/Manager |
| ПланОбмена | ПланОбменаСсылка/Объект/Менеджер↔ExchangePlanRef/Object/Manager |
| БизнесПроцесс | БизнесПроцессСсылка/Объект/Менеджер↔BusinessProcessRef/Object/Manager, БизнесПроцессТочкаМаршрутаСсылка↔BusinessProcessRoutePointRef |
| Задача | ЗадачаСсылка/Объект/Менеджер↔TaskRef/Object/Manager |
| РегистрСведений | РегистрСведенийНаборЗаписей↔InformationRegisterRecordSet, РегистрСведенийКлючЗаписи↔…RecordKey, РегистрСведенийМенеджерЗаписи↔…RecordManager, РегистрСведенийВыборка↔…Selection, РегистрСведенийСписок↔…List, РегистрСведенийМенеджер↔…Manager |
| РегистрНакопления | те же 6 (Accumulation…) |
| РегистрБухгалтерии | те же 6 (Accounting…) |
| РегистрРасчета | те же 6 (Calculation…) + РегистрРасчетаПерерасчет↔CalculationRegisterRecalculation |
| Константа | КонстантаМенеджер↔ConstantManager, КонстантаМенеджерЗначения↔ConstantValueManager |
| Обработка | ОбработкаОбъект↔DataProcessorObject, ОбработкаМенеджер↔DataProcessorManager |
| Отчет | ОтчетОбъект↔ReportObject, ОтчетМенеджер↔ReportManager |
| ОпределяемыйТип | ОпределяемыйТип↔DefinedType |
| ХранилищеНастроек | ХранилищеНастроекМенеджер↔SettingsStorageManager |

Фильтрация по контексту вызова:
- **Тип реквизита прикладного объекта** — примитивы (см. §2.1, флаг «оба») + СправочникСсылка/ДокументСсылка/ПеречислениеСсылка/ОпределяемыйТип/… (только `*Ссылка` и `ОпределяемыйТип`).
- **Тип реквизита формы** — всё из §2.1 + всё из §2.2 + DynamicList.
- **Источник подписки на событие** — только `*Объект`, `*НаборЗаписей`, `КонстантаМенеджерЗначения`.
- **Тип параметра команды** — только `*Ссылка` и `ОпределяемыйТип`.

### 2.3. Квалификаторы

| Канон (рус) | XML | Где |
|---|---|---|
| Длина | length | Строка |
| ДопустимаяДлина | allowedLength: `Переменная` / `Фиксированная` | Строка |
| Разрядность | digits | Число |
| РазрядностьДробнойЧасти | fractionDigits | Число |
| ДопустимыйЗнак | allowedSign: `Любой` / `Неотрицательный` | Число |
| СоставДаты | dateFractions: `Дата` / `Время` / `ДатаВремя` | Дата |

### 2.4. UUID — открытый вопрос

В платформе 8.3 `УникальныйИдентификатор` — полноценный платформенный тип
(`UUID` в XDTO). Однако реальная XML-сериализация в составе реквизита
прикладного объекта неизвестна — нужен один эксперимент в реальной 1С:
создать реквизит типа `УникальныйИдентификатор`, выгрузить XML, посмотреть
точный токен (`<v8:Type>v8:UUID</v8:Type>` или `xs:string` с длиной 36).
Этот шаг — часть **E2** ниже.

---

## Глава 3. Изменения в коде по этапам

### E1. Новый модуль `domain/CanonicalNames.ts`

Единый источник правды:
- `ROOT_KIND_MAP` — по `MetaKind`: каноничный сегмент (мн.ч.), XML-папка, английский тип.
- `CHILD_DIRECT_KINDS` — `ChildTag`, которые добавляются напрямую (Attribute, TabularSection, Dimension, Resource, EnumValue, Column-внутри-ТЧ, Graph).
- `CHILD_SEGMENT_MAP` — `ChildTag → segment` для сегментированных детей.
- `MODULE_SEGMENT_MAP` — `ModuleSlot → segment` + относительный путь в каталоге объекта.
- `REF_KIND_MAP` — все 50+ ссылочных производных (Catalog/Document/Register/…).
- `BASE_TYPES` — массив с полем `contexts: ('metadataAttribute'|'formAttribute')[]`.
- Чистые функции:
  - `canonicalRootPath(kind, name)`
  - `canonicalChildPath(rootKind, rootName, childTag, childName, opts?)`
  - `canonicalModulePath(rootKind, rootName, slot, opts?)`
  - `parseCanonicalPath(input)` — возвращает дискриминированный union `{ kind: 'root'|'child'|'module'|'subsystem', ... }`.
- Все маппинги — readonly; импортируется из `domain/MetaTypes.ts` и `domain/ChildTag.ts`.

Тесты: round-trip `parse(canonicalRootPath(kind, name)) ↔ {kind, name}` для всех типов.

### E2. Платформенный реестр типов

Новый файл `infra/xml/PlatformTypeRegistry.ts`:
- Массив `BASE_TYPES` с `contexts`.
- Функция-генератор производных ссылочных групп по `parseConfigXml`.
- `getTypeRegistry(sourceXmlPath, context)` — возвращает `TypeRegistryTreeGroup[]`.

Правки `TypeRegistryService.ts`:
- Перенести жёстко-вшитые типы оттуда в `PlatformTypeRegistry`.
- Добавить параметр `context: 'metadataAttribute' | 'formAttribute' | 'commandParameter' | 'eventSource'`.
- Расширить `getConfigurationTypes` — выдавать не только `*Ссылка`, но и `*Объект`, `*Менеджер`, `*НаборЗаписей`, `*Выборка`, `*Список`, `*КлючЗаписи`, `*МенеджерЗаписи`.

Правки `MetadataTypeService.ts`:
- `DISPLAY_BY_CANONICAL`, `REF_PREFIXES`, `MANUAL_REFERENCE_PRESENTATIONS` — снести.
- Заменить чтением из `CanonicalNames.REF_KIND_MAP` и `BASE_TYPES`.

**Эмпирический шаг:** перед мерджом — реальный эксперимент с типом `УникальныйИдентификатор` в 1С 8.3, выгрузить XML, зафиксировать сериализацию.

### E3. Перепись `McpMetadataPathService.ts`

Удалить:
- Множественные алиасы (`Catalog.X`, `Catalogs.X`, `Справочник.X`).
- Direct-child shortcut был с пробельными лейблами — заменить на правила §1.3-§1.4.
- Карта `CHILD_GROUP_PATH_LABELS` с ad-hoc маппингами — заменить чтением из `CanonicalNames`.
- Удвоение `Subsystem.Subsystem.X` в путях подсистем.

Реализовать:
- `getCanonicalPath(node)` — строит ровно одну каноническую форму.
- `resolveNode(path)` — нормализует через `parseCanonicalPath` + строгая проверка наличия в дереве.
- На вход принимаются только канонические русские формы. Английские — отклоняются с понятным сообщением «используй канон».
- Выдача `search/list` — только канон.

### E4. Новый MCP-инструмент `v8vscedit_tree`

Без него агент тонет в пагинациях на больших конфигурациях.

```typescript
{
  configuration: string,
  scope?: 'all' | 'objects' | 'common' | <корневой сегмент>,
  depth?: 'roots' | 'objects' | 'children',
  include?: string[]   // фильтр по корневым сегментам ('Справочники', 'Документы', …)
}
```

Возвращает плоский массив строк-канонических-путей, без свойств. Для конфигурации в 5000 объектов с `depth=objects` — порядка 50-100 KB.

### E5. Унификация сигнатур существующих инструментов

Один аргумент `path` везде вместо `objectPath`/`formPath`/`templatePath`/`subsystemPath`/`ciPath`/`rightsPath`/`modulePath`.

| Tool | Было | Стало |
|---|---|---|
| `metadata_info` | `objectPath: <Catalogs/X.xml \| Catalog.X \| …>` | `path: 'Справочники.X'` |
| `validate_metadata` | то же | `path: 'Справочники.X'` (несколько — массив, не pipe) |
| `form_info`/`validate_form`/`edit_form`/`compile_form` | `formPath: …/Form.xml` | `path: 'Справочники.X.Форма.Y'` |
| `mxl_info`/`compile_mxl`/`decompile_mxl`/`validate_mxl` | `templatePath` | `path: 'Справочники.X.Макет.Y'` |
| `skd_info`/`compile_skd`/`edit_skd`/`validate_skd` | `templatePath` | `path: 'Отчеты.X.Макет.ОсновнаяСхема'` |
| `role_info`/`validate_role`/`edit_role` | `rightsPath` | `path: 'Роли.X'` |
| `compile_role` | `outputDir + definition.name` | `parentPath?: 'Конфигурация'` (default), `definition.name` |
| `compile_subsystem` | `outputDir + parentPath` | `parentPath: 'Конфигурация' \| 'Подсистема.X.Y'` |
| `subsystem_info`/`validate_subsystem` | `subsystemPath` | `path: 'Подсистема.X.Y'` |
| `cfe_patch_method` | `modulePath: 'Catalog.X.ObjectModule'` | `path: 'Справочники.X.МодульОбъекта'` |
| `add_help` | `objectPath` | `path: 'Справочники.X'` |
| `edit_command_interface`/`validate_command_interface` | `ciPath` | `path: 'Подсистема.X.КоманднаяПанель'` (новый сегмент или внутреннее свойство) |

Старые имена аргументов **полностью убираются** (по решению пользователя об отсутствии legacy).

### E6. Дополнительные инструменты

| Tool | Что делает |
|---|---|
| `v8vscedit_tree` | См. E4. |
| `v8vscedit_resolve_path` | Принимает строку, возвращает каноническую форму или ошибку с подсказкой. Полезен для отладки. |
| `v8vscedit_list_supported_types` | Реестр платформенных типов с фильтром по контексту (без обращения к конфигурации). |

Удалить:
- `v8vscedit_list_configurations` — дублирует `workspace_overview`.
- `v8vscedit_list_metadata_types` — реестр зашит в этом плане и в описаниях.

### E7. Документация и тесты

- `docs/mcp-paths.md` — копия глав 1-2 этого плана.
- `AGENTS.md` — раздел «MCP-инструменты» переписать со ссылкой на справочник.
- Все описания tools укоротить до 5-7 строк + один пример пути. Без полотен текста.
- Тесты `mcpMetadataPathService.test.ts` — переписать на канон, удалить тесты на старые алиасы.
- Тесты `mcpPropertyService.test.ts` — добавить кейсы UUID/Тип/ОписаниеТипов/СписокЗначений (последний — отбивается для реквизита справочника).
- Новый `canonicalNames.test.ts` — round-trip и таблицы маппинга.

---

## Глава 4. Очерёдность реализации

| # | Этап | Объём | Зависимости |
|---|---|---|---|
| 1 | **E1** `domain/CanonicalNames.ts` + расширение `MetaTypeDef`/`ChildTagConfig` | ~500 строк | — |
| 2 | **E2** `PlatformTypeRegistry` + перепись `TypeRegistryService`/`MetadataTypeService` | ~400 строк | E1 |
| 3 | **E3** Перепись `McpMetadataPathService` | ~600 строк | E1 |
| 4 | **E5** Унификация сигнатур tools (breaking) | ~300 строк по `V8McpServer.ts` | E1, E3 |
| 5 | **E4 + E6** Новые tools `tree`, `resolve_path`, `list_supported_types` | ~250 строк | E3 |
| 6 | **E7** Документация, тесты | — | всё выше |

Между этапами — `npm run compile` и `npm run lint` без ошибок (`--max-warnings=0`).

---

## Глава 5. Граничные случаи

| Случай | Решение |
|---|---|
| Реквизит и табличная часть с одинаковым именем | Запрещено платформой — резолвер падает с понятной ошибкой. |
| Объект и общий модуль с одинаковым именем в разных корнях | Уже разнесены по корневым сегментам (`Справочники.X` vs `ОбщиеМодули.X`). |
| Расширение CFE с заимствованным объектом | Корневой сегмент тот же (`Справочники.X`), плюс свойство `borrowed: true` в DTO выдачи. Путь не меняется. |
| Несколько конфигураций в workspace | Аргумент `configuration` обязателен. Сообщение об ошибке перечисляет доступные имена. |
| Точка внутри имени объекта | Невозможно по правилам 1С — резолвер падает на парсинге. |
| Пробел в имени объекта | Платформа допускает, но конвертер EDT обычно не пускает. Резолвер принимает «как есть», но в выдаче окружает в кавычки: `Справочники."Имя с пробелом"`. (Маркер опасности — лучше переименовать.) |
