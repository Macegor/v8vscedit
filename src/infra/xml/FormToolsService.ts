/**
 * Реэкспорт для обратной совместимости. Реализация перенесена в
 * подкаталог `./form/`, где разделена на специализированные сервисы
 * (Add/Compile/Edit/Info/Validate) — это упрощает порт логики из
 * соответствующих скиллов `form-*`.
 */
export * from './form';
