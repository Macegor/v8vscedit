import { NodeDescriptor } from '../_types';

export const DocumentDescriptor: NodeDescriptor = {
  icon: 'document',
  folderName: 'Documents',
  children: ['Attribute', 'TabularSection', 'Form', 'Command', 'Template'],
};

