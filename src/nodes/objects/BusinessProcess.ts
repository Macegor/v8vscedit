import { NodeDescriptor } from '../_types';

export const BusinessProcessDescriptor: NodeDescriptor = {
  icon: 'businessProcess',
  folderName: 'BusinessProcesses',
  children: ['Attribute', 'TabularSection', 'Form', 'Command'],
};

