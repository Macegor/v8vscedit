import { NodeDescriptor } from '../_types';

/** Универсальная группа для сгруппированных элементов (реквизиты, формы и т.п.) */
export const groupTypeDescriptor: NodeDescriptor = {
  icon: 'folder',
};

