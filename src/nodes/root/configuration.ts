import { NodeDescriptor } from '../_types';

/** Корневой узел основной конфигурации */
export const configurationDescriptor: NodeDescriptor = {
  icon: 'common',
  contextMenuCommands: ['openXmlFile'],
};

