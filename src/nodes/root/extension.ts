import { NodeDescriptor } from '../_types';

/** Корневой узел расширения конфигурации */
export const extensionDescriptor: NodeDescriptor = {
  icon: 'editObject',
  contextMenuCommands: ['openXmlFile'],
};

